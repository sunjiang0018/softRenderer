export default class Spherical2D {
    p:number
    theta:number
    phi:number

    constructor(p:number, theta:number, phi:number) {
        this.p = p
        this.theta = theta
        this.phi = phi
    }
}
